trigger:
- master

# strategy:
#   matrix:
#     linux:
#       operatingSystem: 'devmaster'
#     mac:
#       operatingSystem: 'devmaster'

pool: devmaster

steps:
- script: echo Running on $(operatingSystem)
  displayName: 'Run a one-line script'
